import { DatetimeDatePipe } from './datetime-date.pipe';

describe('DatetimeDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeDatePipe();
    expect(pipe).toBeTruthy();
  });
});
