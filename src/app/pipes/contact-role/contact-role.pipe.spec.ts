import { ContactRolePipe } from './contact-role.pipe';

describe('ContactRolePipe', () => {
  it('create an instance', () => {
    const pipe = new ContactRolePipe();
    expect(pipe).toBeTruthy();
  });
});
