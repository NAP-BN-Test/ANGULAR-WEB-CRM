import { DatetimeTimeDefaultPipe } from './datetime-time-default.pipe';

describe('DatetimeTimeDefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeTimeDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
