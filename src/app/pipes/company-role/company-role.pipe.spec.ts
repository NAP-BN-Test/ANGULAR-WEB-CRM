import { CompanyRolePipe } from './company-role.pipe';

describe('CompanyRolePipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyRolePipe();
    expect(pipe).toBeTruthy();
  });
});
