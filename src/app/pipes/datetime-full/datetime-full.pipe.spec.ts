import { DatetimeFullPipe } from './datetime-full.pipe';

describe('DatetimeFullPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeFullPipe();
    expect(pipe).toBeTruthy();
  });
});
