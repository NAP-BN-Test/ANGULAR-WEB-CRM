import { DatetimeTimePipe } from './datetime-time.pipe';

describe('DatetimeTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeTimePipe();
    expect(pipe).toBeTruthy();
  });
});
